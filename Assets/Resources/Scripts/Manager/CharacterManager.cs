using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterManager : MonoBehaviour
{
    public UnityEditor.U2D.Animation.CharacterData[] CharacterKeys = new UnityEditor.U2D.Animation.CharacterData[GameManager.instance.Setting.CharacterCount];
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
